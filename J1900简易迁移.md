# 🚀 J1900 超简单迁移指南

## 📦 第一步：打包（在 Windows 上）

```powershell
# 运行打包脚本
cd D:\招生系统
.\scripts\prepare_migration.ps1
```

**结果：** 在 `D:\招生系统_迁移包` 生成迁移文件

---

## 💾 第二步：传输到 J1900

**用 U盘：**
1. 复制 `D:\招生系统_迁移包` 到 U盘
2. 在 J1900 上插入 U盘，复制到 `/home/user/`

---

## 🔧 第三步：在 J1900 上部署

```bash
# 1. 解压项目
cd ~
unzip 招生系统_迁移包/crm_project_*.zip -d 招生系统

# 2. 复制数据库
cp 招生系统_迁移包/crm_*.db ~/招生系统/crm.db

# 3. 一键部署（会自动安装所有依赖 + 数据库迁移）
cd ~/招生系统
chmod +x deploy.sh start.sh stop.sh
./deploy.sh

# 4. 启动服务
./start.sh
```

**就这么简单！** 🎉

---

## 🆘 如果遇到 500 错误

如果启动后访问报 500 错误，说明数据库 schema 需要更新：

```bash
cd ~/招生系统

# 手动运行数据库迁移脚本
export SECRET_KEY=$(cat .secret_key)
.venv/bin/python scripts/migrate_db_schema.py

# 重启服务
./stop.sh && ./start.sh
```

**原因：** 新版本代码增加了字段（token_version, last_login_device 等），旧数据库需要迁移。

---

## 📱 访问系统

打开浏览器访问：
```
http://j1900的IP地址:8000
```

默认账号：
- 用户名：`admin`
- 密码：`admin123`

---

## 🔄 日常使用

```bash
# 启动服务
./start.sh

# 停止服务
./stop.sh

# 查看日志
tail -f backend.log

# 重启服务
./stop.sh && ./start.sh
```

---

## ⚙️ 可选：配置开机自启

```bash
# 编辑 crontab
crontab -e

# 添加这一行
@reboot cd /home/user/招生系统 && ./start.sh
```

---

## 🆘 常见问题

### 问题1：访问报 500 错误
```bash
# 运行数据库迁移脚本
cd ~/招生系统
export SECRET_KEY=$(cat .secret_key)
.venv/bin/python scripts/migrate_db_schema.py
./stop.sh && ./start.sh
```

### 问题2：端口被占用
```bash
# 查看占用端口的进程
sudo lsof -i :8000

# 杀掉进程
sudo kill -9 进程ID
```

### 问题3：Python 版本不对
```bash
# 安装 Python 3.11
sudo apt install python3.11 python3.11-venv
```

### 问题4：Node.js 未安装
```bash
# 安装 Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

---

**就这么简单！不需要 Nginx、Supervisor 等复杂配置。**

**直接运行 `./start.sh` 就可以了！** ✨
